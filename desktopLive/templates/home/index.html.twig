<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LiveShop - Expérience de Shopping en Direct</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #4F46E5;
            --primary-dark: #4338CA;
            --secondary: #FF6B6B;
            --dark: #1F2937;
            --light: #F9FAFB;
            --gray: #E5E7EB;
            --accent: #10B981;
            --text: #111827;
            --text-light: #6B7280;
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--light);
            color: var(--text);
            line-height: 1.6;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1.5rem;
        }
        
        header {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            padding: 1.5rem 0;
            position: relative;
            overflow: hidden;
        }
        
        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .logo {
            font-size: 1.8rem;
            font-weight: 800;
            display: flex;
            align-items: center;
        }
        
        .logo i { margin-right: 0.5rem; color: var(--secondary); }
        
        nav ul { display: flex; list-style: none; }
        nav ul li { margin-left: 2rem; }
        nav ul li a {
            color: white;
            text-decoration: none;
            font-weight: 500;
            transition: opacity 0.3s;
        }
        nav ul li a:hover { opacity: 0.8; }
        
        .header-actions {
            display: flex;
            align-items: center;
        }
        
        .header-actions button {
            background: rgba(255, 255, 255, 0.2);
            border: none;
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 4px;
            margin-left: 1rem;
            cursor: pointer;
            transition: background 0.3s;
        }
        .header-actions button:hover { background: rgba(255, 255, 255, 0.3); }
        
        .hero {
           background: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), 
            url('Uploads/hero.png') center/cover no-repeat;
            color: white;
            padding: 12rem 0;
            text-align: center;
            position: relative;
        }
        
        .hero-content { max-width: 800px; margin: 0 auto; }
        .hero h1 { font-size: 3.5rem; font-weight: 800; margin-bottom: 1rem; line-height: 1.2; }
        .hero p { font-size: 1.5rem; margin-bottom: 2rem; opacity: 0.9; }
        .price-tag {
            background: var(--secondary);
            color: white;
            display: inline-block;
            padding: 0.5rem 1.5rem;
            border-radius: 30px;
            font-weight: 700;
            font-size: 1.8rem;
            margin: 1rem 0;
        }
        .cta-button {
            background: var(--primary);
            color: white;
            border: none;
            padding: 1rem 2.5rem;
            font-size: 1.2rem;
            font-weight: 600;
            border-radius: 8px;
            cursor: pointer;
            transition: background 0.3s, transform 0.3s, box-shadow 0.3s;
            margin-top: 1rem;
            position: relative;
            overflow: hidden;
        }
        .cta-button::after {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: rgba(255,255,255,0.2);
            transform: skewX(-25deg);
            transition: 1.5s;
        }
        .cta-button:hover::after { left: 200%; }
        .cta-button:hover {
            background: var(--primary-dark);
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.2);
        }
        
        .live-badge {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: red;
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 4px;
            font-weight: 600;
            display: flex;
            align-items: center;
            animation: pulse 1.5s infinite;
        }
        .live-badge i { margin-right: 0.5rem; }
        @keyframes pulse { 0% { opacity: 1; } 50% { opacity: 0.7; } 100% { opacity: 1; } }

        .section-title {
            text-align: center;
            margin: 3rem 0 2rem;
            font-size: 2rem;
            font-weight: 700;
            color: var(--dark);
        }
        
        .featured-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-bottom: 4rem;
        }
        
        .product-card {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            transition: transform 0.3s, box-shadow 0.3s;
        }
        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }
        .product-image {
            height: 200px;
            background: var(--gray);
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
        }
        .product-image img { max-width: 100%; max-height: 100%; object-fit: cover; }
        .product-info { padding: 1.5rem; }
        .product-title { font-weight: 600; margin-bottom: 0.5rem; font-size: 1.1rem; }
        .product-desc { color: var(--text-light); font-size: 0.9rem; margin-bottom: 1rem; }
        .product-price { font-weight: 700; color: var(--primary); font-size: 1.2rem; }

        .promo-section {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            padding: 3rem 0;
            text-align: center;
            margin: 3rem 0;
            border-radius: 12px;
        }
        .promo-content { max-width: 800px; margin: 0 auto; }
        .promo-title { font-size: 2.2rem; margin-bottom: 1rem; }
        .promo-desc { font-size: 1.2rem; margin-bottom: 1.5rem; opacity: 0.9; }

        .weekly-deals {
            background: white;
            border-radius: 12px;
            padding: 2rem;
            margin: 3rem 0;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        }
        .deals-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem; }
        .deals-title { font-size: 1.8rem; font-weight: 700; }
        .deals-countdown {
            background: var(--gray);
            padding: 0.5rem 1rem;
            border-radius: 4px;
            font-weight: 600;
        }
        .deals-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1.5rem; }

        .discount-banner {
            background: linear-gradient(to right, var(--secondary), #FF8E8E);
            color: white;
            padding: 2rem;
            border-radius: 12px;
            text-align: center;
            margin: 3rem 0;
        }
        .discount-title { font-size: 2rem; margin-bottom: 1rem; }
        .discount-amount { font-size: 3rem; font-weight: 800; margin-bottom: 1rem; }

        .live-stream {
            margin: 4rem 0;
            background: var(--dark);
            color: white;
            border-radius: 12px;
            overflow: hidden;
        }
        .stream-header {
            padding: 1.5rem;
            background: rgba(0, 0, 0, 0.4);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .stream-title { font-size: 1.5rem; font-weight: 600; }
        .stream-viewers { display: flex; align-items: center; }
        .stream-viewers i { margin-right: 0.5rem; color: red; }
        .stream-content {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 2rem;
        }
        .stream-video {
            width: 100%;
            height: 400px;
            background: #000;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1.5rem;
            position: relative;
        }
        .stream-video iframe {
            width: 100%;
            height: 100%;
            border: none;
        }
        .stream-products {
            width: 100%;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
        }
        .cart-btn {
            background: #4F46E5;
            border: none;
            border-radius: 10%;
            width: 40px;
            height: 40px;
            color: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: background 0.3s ease, transform 0.2s ease;
            position: relative;
            overflow: hidden;
        }
        .cart-btn i {
            font-size: 18px;
            transition: transform 0.4s ease;
        }
        .cart-btn:hover {
            background: #4338CA;
            transform: scale(1.1);
        }
        .cart-btn.added {
            background: #22c55e;
        }
        .cart-btn.added i {
            animation: tiltCart 0.4s ease;
        }
        @keyframes tiltCart {
            0% { transform: rotate(0deg); }
            25% { transform: rotate(15deg); }
            75% { transform: rotate(-15deg); }
            100% { transform: rotate(0deg); }
        }
        .cart-btn.disabled {
            background: #6B7280;
            cursor: not-allowed;
            transform: none;
        }
        .cart-message {
            display: none;
            position: absolute;
            bottom: -30px;
            left: 50%;
            transform: translateX(-50%);
            background: #333;
            color: #fff;
            padding: 5px 10px;
            border-radius: 4px;
            font-size: 12px;
            white-space: nowrap;
            z-index: 10;
        }

        footer {
            background: var(--dark);
            color: white;
            padding: 3rem 0;
            margin-top: 4rem;
        }
        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
        }
        .footer-section h3 {
            font-size: 1.2rem;
            margin-bottom: 1.5rem;
            position: relative;
            padding-bottom: 0.5rem;
        }
        .footer-section h3::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 50px;
            height: 2px;
            background: var(--primary);
        }
        .footer-links { list-style: none; }
        .footer-links li { margin-bottom: 0.8rem; }
        .footer-links a { color: var(--gray); text-decoration: none; transition: color 0.3s; }
        .footer-links a:hover { color: white; }
        .social-icons {
            display: flex;
            margin-top: 1.5rem;
        }
        .social-icons a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            margin-right: 1rem;
            color: white;
            transition: background 0.3s;
        }
        .social-icons a:hover { background: var(--primary); }

        .copyright {
            text-align: center;
            margin-top: 3rem;
            padding-top: 1.5rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: var(--text-light);
            font-size: 0.9rem;
        }

        @media (max-width: 768px) {
            .header-content { flex-direction: column; text-align: center; }
            nav ul { margin: 1.5rem 0; }
            .hero h1 { font-size: 2.5rem; }
            .hero p { font-size: 1.2rem; }
            .featured-grid, .deals-grid { grid-template-columns: 1fr; }
            .stream-video { height: 300px; }
        }
    </style>
</head>
<body>
    {% include 'components/navbarClient.html.twig' %}

    <section class="hero">
        <div class="container">
            <div class="hero-content">
                <h1>Découvrez le Shopping en Direct</h1>
                <p>Des offres exclusives, en temps réel</p>
                <form action="{{ path('app_client') }}" method="get" style="display: inline;">
                    <button type="submit" class="cta-button">
                        Rejoindre le Live <i class="fas fa-arrow-right"></i>
                    </button>
                </form>
            </div>
        </div>
    </section>

    <main class="container">
        {# --- REAL DATA --- #}
        {# Variables attendues : user, favorites, cart, products, categories #}

        <div style="display:flex; gap:2rem; margin-bottom:2rem; margin-top:2rem;">
            <div style="flex:1; background:#fff; border-radius:12px; box-shadow:0 2px 8px #eee; padding:1.5rem; display:flex; align-items:center; justify-content:space-between;">
                <div style="display:flex; align-items:center; gap:2rem;">
                    <div style="display:flex; align-items:center; gap:8px;">
                        <i class="fa-solid fa-bookmark" style="color:#FFD600; font-size:22px;"></i>
                        <span style="font-weight:600;">Favoris</span>
                        <span class="badge" style="background:#FFD600; color:#231f20; border-radius:12px; padding:4px 12px; font-weight:700; margin-left:4px;">{{ favorites|length }}</span>
                    </div>
                    <div style="display:flex; align-items:center; gap:8px;">
                        <i class="fa-solid fa-shopping-cart" style="color:#4F46E5; font-size:22px;"></i>
                        <span style="font-weight:600;">Panier</span>
                        <span class="badge cart-count" style="background:#4F46E5; color:#fff; border-radius:12px; padding:4px 12px; font-weight:700; margin-left:4px;">{{ cart|length }}</span>
                    </div>
                    <div style="display:flex; align-items:center; gap:8px;">
                        <i class="fa-solid fa-gift" style="color:#10B981; font-size:22px;"></i>
                        <span style="font-weight:600;">Fidélité</span>
                        <span class="badge" style="background:#10B981; color:#fff; border-radius:12px; padding:4px 12px; font-weight:700; margin-left:4px;">{{ user.fidelity|default(0) }}</span>
                    </div>
                </div>
            </div>
        </div>

        <div style="display:flex; gap:2rem;">
            <aside style="width:260px; background:#fff; border-radius:12px; box-shadow:0 2px 8px #eee; padding:1.5rem;">
                <h4 style="font-size:1.1rem; font-weight:700; margin-bottom:1.2rem;">Filtrer</h4>
                <form id="filter-form" style="display:flex; flex-direction:column; gap:1rem;">
                    <label for="cat">Catégorie :</label>
                    <select id="cat" name="cat" style="padding:6px 12px; border-radius:6px;">
                        <option value="">Toutes</option>
                        {% for cat in categories %}
                            <option value="{{ cat }}">{{ cat }}</option>
                        {% endfor %}
                    </select>
                    <label for="minprice">Prix min :</label>
                    <input type="number" id="minprice" name="minprice" min="0" style="width:100%; padding:6px 8px; border-radius:6px;">
                    <label for="maxprice">Prix max :</label>
                    <input type="number" id="maxprice" name="maxprice" min="0" style="width:100%; padding:6px 8px; border-radius:6px;">
                    <button type="submit" style="background:#4F46E5; color:#fff; border:none; border-radius:6px; padding:8px 0; font-weight:600; margin-top:1rem;">Filtrer</button>
                </form>
            </aside>
            <section style="flex:1;">
                <h2 class="section-title">Liste des produits</h2>
                <div class="featured-grid" id="product-list">
                    {% for product in products %}
                    <div class="product-card" data-cat="{{ product.category|default('') }}" data-price="{{ product.price|default(0) }}" style="position:relative; min-width:260px; max-width:340px; margin:auto;">
                        <div class="product-image" style="height:180px;">
                            {% if product.images %}
                                <img src="/Uploads/{{ product.images }}" alt="{{ product.name }}" style="width:100%; height:100%; object-fit:cover;">
                            {% else %}
                                <i class="fas fa-gift fa-3x" style="color: #4F46E5;"></i>
                            {% endif %}
                        </div>
                        <div class="product-info">
                            <div class="product-rating" style="margin-bottom:6px;">
                                <span style="font-size:13px; color:#888; margin-left:6px;">Rating : </span>
                                {% set rating = product.rating|default(0) %}
                                {% for i in 1..5 %}
                                    <i class="fa-star{% if i <= rating %} fas{% else %} far{% endif %}" style="color:#FFD600; font-size:18px;"></i>
                                {% endfor %}
                            </div>
                            <h3 class="product-title" style="font-size:1.15rem;">{{ product.name }}</h3>
                            <div style="font-size:0.95rem; color:#888; margin-bottom:4px;">Vendeur : {{ product.seller|default('N/A') }}</div>
                            <div class="product-price" style="margin-bottom:8px;">${{ product.price }}</div>
                            <div style="margin:8px 0; display:flex; align-items:center; gap:12px; flex-wrap:wrap; position: relative;">
                                <button class="fav-btn" data-id="{{ product.id }}" style="background:none; border:none; cursor:pointer;" title="Ajouter aux favoris">
                                    <i class="fa{{ product.id in favorites ? 's' : 'r' }} fa-bookmark" style="color:#FFD600; font-size:22px;"></i>
                                </button>
                                <form method="post" action="{{ path('app_client_add_cart', {'id': product.id}) }}" style="display:inline;">
                                    <input type="hidden" name="name" value="{{ product.name }}">
                                    <input type="hidden" name="price" value="{{ product.price }}">
                                    <input type="hidden" name="images" value="{{ product.images }}">
                                    <input type="hidden" name="quantity" value="1">
                                    <button type="submit" class="cart-btn {{ product.id in cart ? 'disabled' : '' }}" title="{{ product.id in cart ? 'Déjà dans le panier' : 'Ajouter au panier' }}" {{ product.id in cart ? 'disabled' : '' }}>
                                        <i class="fas fa-shopping-basket"></i>
                                        <span class="cart-message">Déjà dans le panier</span>
                                    </button>
                                </form>
                            </div>
                            <div style="display:flex; gap:8px; flex-wrap:wrap; margin-top:8px;">
                                {% if product.badges is defined %}
                                    {% for badge in product.badges %}
                                        <span style="background:{{ badge.color|default('#FFD600') }}; color:#231f20; border-radius:8px; padding:2px 10px; font-size:12px; font-weight:600;">{{ badge.label }}</span>
                                    {% endfor %}
                                {% endif %}
                            </div>
                        </div>
                        {% if product.id in favorites %}
                            <span style="position:absolute; top:18px; right:18px; background:#FFD600; color:#231f20; border-radius:50%; padding:6px 10px; font-weight:700; font-size:13px;">Favori</span>
                        {% endif %}
                        {% if product.id in cart %}
                            <span style="position:absolute; top:48px; right:18px; background:#4F46E5; color:#fff; border-radius:50%; padding:6px 10px; font-weight:700; font-size:13px;">Panier</span>
                        {% endif %}
                    </div>
                    {% endfor %}
                </div>
                <div style="margin:2rem 0; background:#F9FAFB; border-radius:12px; padding:2rem; text-align:center;">
                    <h3 style="font-size:1.3rem; font-weight:700; margin-bottom:1rem;">Fidélité</h3>
                    <p>Point de fidélité après achat.<br>Les points deviennent des remises !</p>
                    <div style="font-size:1.1rem; color:#10B981; font-weight:700; margin-top:1rem;">
                        Points cumulés : {{ user.fidelity|default(0) }}
                    </div>
                </div>
                <script>
document.addEventListener('DOMContentLoaded', function() {
    const filterForm = document.getElementById('filter-form');
    const productList = document.getElementById('product-list');

    // --- Filtrage local ---
    if (filterForm && productList) {
        filterForm.addEventListener('submit', function(e) {
            e.preventDefault();
            const cat = document.getElementById('cat').value.toLowerCase();
            const minprice = parseFloat(document.getElementById('minprice').value) || 0;
            const maxprice = parseFloat(document.getElementById('maxprice').value) || Infinity;
            productList.querySelectorAll('.product-card').forEach(function(card) {
                const cardCat = card.getAttribute('data-cat').toLowerCase();
                const cardPrice = parseFloat(card.getAttribute('data-price')) || 0;
                let show = true;
                if (cat && cardCat !== cat) show = false;
                if (cardPrice < minprice) show = false;
                if (cardPrice > maxprice) show = false;
                card.style.display = show ? '' : 'none';
            });
            productList.scrollIntoView({ behavior: 'smooth', block: 'start' });
        });

        // --- Filtrage AJAX ---
        filterForm.addEventListener('submit', function(e) {
            e.preventDefault();
            const formData = new FormData(filterForm);
            const params = new URLSearchParams(formData).toString();
            fetch(window.location.pathname + '?' + params, {
                headers: { 'X-Requested-With': 'XMLHttpRequest' }
            })
            .then(res => res.text())
            .then(html => {
                const tempDiv = document.createElement('div');
                tempDiv.innerHTML = html;
                const newList = tempDiv.querySelector('#product-list');
                if (newList) {
                    productList.innerHTML = newList.innerHTML;
                    productList.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });
    }

    // --- Favoris et Panier dynamique ---
    if (productList) {
        productList.addEventListener('click', function(e) {
            // Favoris
            if (e.target.closest('.fav-btn')) {
                const btn = e.target.closest('.fav-btn');
                btn.classList.toggle('active');
                btn.querySelector('i').classList.toggle('fas');
                btn.querySelector('i').classList.toggle('far');
            }

            // Panier dynamique
            if (e.target.closest('.cart-btn')) {
                e.preventDefault();
                const form = e.target.closest('form');
                const btn = form.querySelector('.cart-btn');
                const icon = btn.querySelector('i');
                const cartBadge = document.querySelector('.cart-count');

                if (btn.classList.contains('disabled')) {
                    const message = btn.querySelector('.cart-message');
                    message.style.display = 'block';
                    setTimeout(() => { message.style.display = 'none'; }, 2000);
                    return;
                }

                let cartCount = parseInt(cartBadge.textContent) || 0;

                // Animation visuelle - Inclinaison du panier
                btn.classList.add('added');
                setTimeout(() => {
                    btn.classList.remove('added');
                    btn.classList.add('disabled');
                    btn.disabled = true;
                    btn.setAttribute('title', 'Déjà dans le panier');
                }, 800);

                // Mise à jour du compteur panier
                cartBadge.textContent = cartCount + 1;

                // Envoi AJAX
                fetch(form.action, {
                    method: "POST",
                    body: new FormData(form)
                })
                .then(res => {
                    if (!res.ok) throw new Error("Erreur serveur");
                    return res.text();
                })
                .then(data => {
                    console.log("Produit ajouté au panier :", data);
                })
                .catch(err => console.error(err));
            }
        });
    }
});
                </script>
            </section>
        </div>

        <div class="promo-section">
            <div class="promo-content">
                <h2 class="promo-title">Offre Spéciale</h2>
                <p class="promo-desc">Recevez 20% de cashback</p>
                <button class="cta-button">Voir les offres</button>
            </div>
        </div>

        <div class="weekly-deals">
            <div class="deals-header">
                <h2 class="deals-title">Promotions de la Semaine</h2>
                <div class="deals-countdown">
                    <i class="fas fa-clock"></i> 2j 12h 45m
                </div>
            </div>
            <div class="deals-grid">
                <div class="product-card">
                    <div class="product-image">
                        <i class="fas fa-star fa-3x" style="color: #FFD700;"></i>
                    </div>
                    <div class="product-info">
                        <h3 class="product-title">Pack Économique</h3>
                        <p class="product-desc">Quantité: 18 unités</p>
                        <div class="product-price">$199.99</div>
                    </div>
                </div>
                <div class="product-card">
                    <div class="product-image">
                        <i class="fas fa-gem fa-3x" style="color: #10B981;"></i>
                    </div>
                    <div class="product-info">
                        <h3 class="product-title">Édition Limitée</h3>
                        <p class="product-desc">Quantité: 10 unités</p>
                        <div class="product-price">$249.99</div>
                    </div>
                </div>
            </div>
        </div>

        <div class="discount-banner">
            <h2 class="discount-title">Jusqu'à 30% de Réduction</h2>
            <div class="discount-amount">Remise Instantanée</div>
            <button class="cta-button">Profiter des réductions</button>
        </div>

        <div class="live-stream">
            <div class="stream-header">
                <h2 class="stream-title">Live Shopping: Équipement Maison</h2>
                <div class="stream-viewers">
                    <i class="fas fa-users"></i> 1.2K viewers
                </div>
            </div>
            <div class="stream-content">
                <div class="stream-video">
                    <iframe src="https://www.youtube.com/embed/ohmajJTcpNk?autoplay=1&mute=1" allowfullscreen></iframe>
                    <div class="live-badge" style="position: absolute; top: 1rem; right: 1rem;">
                        <i class="fas fa-broadcast-tower"></i> LIVE
                    </div>
                </div>
                <div class="stream-products">
                    <div class="product-card">
                        <div class="product-image">
                            <i class="fas fa-keyboard fa-2x" style="color: #4F46E5;"></i>
                        </div>
                        <div class="product-info">
                            <h3 class="product-title">Clavier</h3>
                            <div class="product-price">$89.99</div>
                        </div>
                    </div>
                    <div class="product-card">
                        <div class="product-image">
                            <i class="fas fa-headphones fa-2x" style="color: #4F46E5;"></i>
                        </div>
                        <div class="product-info">
                            <h3 class="product-title">Casque Audio</h3>
                            <div class="product-price">$129.99</div>
                        </div>
                    </div>
                    <div class="product-card">
                        <div class="product-image">
                            <i class="fas fa-microphone fa-2x" style="color: #4F46E5;"></i>
                        </div>
                        <div class="product-info">
                            <h3 class="product-title">Microphone</h3>
                            <div class="product-price">$79.99</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <h3>À propos de LiveShop</h3>
                    <p>LiveShop révolutionne l'expérience d'achat en direct avec des produits exclusifs et des offres interactives.</p>
                    <div class="social-icons">
                        <a href="#"><i class="fab fa-facebook-f"></i></a>
                        <a href="#"><i class="fab fa-twitter"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                        <a href="#"><i class="fab fa-youtube"></i></a>
                    </div>
                </div>
                <div class="footer-section">
                    <h3>Liens rapides</h3>
                    <ul class="footer-links">
                        <li><a href="#">Accueil</a></li>
                        <li><a href="#">Live Shopping</a></li>
                        <li><a href="#">Produits</a></li>
                        <li><a href="#">Événements</a></li>
                        <li><a href="#">Contact</a></li>
                    </ul>
                </div>
                <div class="footer-section">
                    <h3>Contact</h3>
                    <ul class="footer-links">
                        <li><i class="fas fa-map-marker-alt"></i> 123 Rue du Commerce, Paris</li>
                        <li><i class="fas fa-phone"></i> +33 1 23 45 67 89</li>
                        <li><i class="fas fa-envelope"></i> info@liveshop.fr</li>
                    </ul>
                </div>
            </div>
            <div class="copyright">
                &copy; 2023 LiveShop. Tous droits réservés.
            </div>
        </div>
    </footer>
</body>
</html>